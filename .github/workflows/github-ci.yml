name: CI

on: [push]

jobs:
  build:

    runs-on:
      - ubuntu-latest
      - ubuntu-18.04
      - ubuntu-16.04
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run:
    - name: Run a multi-line script
      run: |
        bash install.sh
